<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cashierapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CashierNative">
        <activity
            android:name="com.cashierapp.Activities.CashierActivity"
            android:label="@string/title_activity_cashiers"
            android:theme="@style/Theme.CashierNative.NoActionBar"></activity>
        <activity
            android:name="com.cashierapp.Activities.CashiersActivity"
            android:label="@string/title_activity_cashiers"
            android:theme="@style/Theme.CashierNative.NoActionBar"></activity>
        <activity android:name="com.cashierapp.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
